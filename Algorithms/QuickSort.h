#pragma once

#include <vector>

void swap(int* a, int* b);

int partition(std::vector<int>& nums, int start, int end);

void quickSort(std::vector<int>& nums, int start, int end);

void printQuick(std::vector<int> nums);